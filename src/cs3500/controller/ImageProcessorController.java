package cs3500.controller;

/**
 * Interface for the controller in an image processor, allows for user input.
 */
public interface ImageProcessorController {

  /**
   * Runs the image processor.
   */
  void startProcessor();
}
